# Build output — built locally by setup.sh / mac/build.sh
/proxy/codex-dictate-proxy
/proxy/voxtype-codex-proxy
/mac/codex-dictate
*.exe

# Never commit secrets / auth
auth.json
.codex/

# Editor / OS noise
*.swp
*~
.DS_Store

# Voxtype config backups produced by setup.sh
*.bak-*
